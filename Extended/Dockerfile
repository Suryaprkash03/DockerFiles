# Use the official Nginx base image from Docker Hub
FROM nginx:latest

# Add metadata to the image (useful for documentation and image management)
LABEL version="1.0"
LABEL maintainer="suryatheking5784@gmail.com"

# Copy the local index.html file to Nginx's default web directory
# This will replace the default Nginx welcome page with your custom page
COPY index.html /usr/share/nginx/html/index.html







